// Settings.json backup. The original settings.json is usually not versioned for ease of use.

{
  "window.title": "${dirty}${activeEditorShort}${separator}${rootName}${separator}${appName} ${separator} [Branch: master]"
}